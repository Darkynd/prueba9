<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="SpinnerTheme" parent="Theme.AppCompat.Light">
        <item name="android:spinnerDropDownItemStyle">@style/SpinnerDropdownItem</item>
        <item name="android:backgroundTint">@color/white</item> <!-- Cambiar el color de fondo del Spinner -->
        <item name="android:textColor">@color/white</item> <!-- Cambiar el color del texto del Spinner -->
    </style>

    <style name="SpinnerDropdownItem" parent="Widget.AppCompat.DropDownItem.Spinner">
        <item name="android:textColor">@color/white</item> <!-- Cambiar el color del texto en el dropdown del Spinner -->
    </style>
</resources>